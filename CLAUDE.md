# PLATFORMANCE — память проекта для Claude

## 🚦 Протокол начала работы («старт работ»)

Когда пользователь пишет **«старт работ»** (или «start», «начинаем», «продолжаем работу») — прежде чем что-либо делать:

1. Прочитай `CHANGELOG.md` (журнал изменений) и раздел «Известные открытые задачи» ниже.
2. Выполни `git log origin/main --oneline -10` — сверь, нет ли в `main` коммитов, которых ещё нет в журнале (если есть — допиши их в журнал).
3. Ответь пользователю коротким брифом: (а) что было сделано в прошлый раз (2–4 пункта из свежих записей журнала), (б) список открытых задач, (в) вопрос «с чего начнём — с одной из открытых задач или с чего-то нового?».
4. Не начинай правки, пока пользователь не выбрал задачу.

## 📓 Правило ведения журнала

**Каждое изменение, попадающее в `main`, фиксируется записью в `CHANGELOG.md` в том же PR** (новые записи сверху: дата, PR/коммит, суть, файлы). Правки без записи в журнале не публиковать.

## О проекте

Статический лендинг PLATFORMANCE (системное ведение магазинов на Wildberries/Ozon).
Чистый HTML/CSS/JS без сборщика, хостится на GitHub Pages.

- Репозиторий: `alexblacklucky/PLATFORMANCE`
- Прод-ветка: `main`
- Основной домен: `https://platformance.ru` — куплен 2026-08-11 на reg.ru, DNS-зона на `ns1/ns2.reg.ru`: 4 A-записи `@` на IP GitHub Pages + CNAME `www` → `alexblacklucky.github.io`. Файл `CNAME` в корне репо. Старый Pages-адрес `https://alexblacklucky.github.io/PLATFORMANCE/` работает через 301-редирект
- Домен `platformance.online` куплен про запас, не настроен (позже — переадресация на основной через услугу reg.ru). Купленные вместе с доменом хостинг Reg.Host и DomainSSL не нужны (хостинг — GitHub Pages, сертификат выпускает GitHub); пользователю рекомендовано отключить их автопродление
- Оператор/владелец: ИП Чалова Татьяна Владимировна

## Структура

- `index.html` — главная; заявка — встроенная CRM-форма Bitrix24 (`script[data-b24-form="inline/4/btnjoi"]` в `#audit .form-card`, слот `.b24-form-slot`, запасные контакты `.b24-fallback`). Старая нативная `.lead-form` — в `snippets/native-lead-form.html`, её логика в `app.js` жива, но неактивна
- `privacy.html` — политика конфиденциальности (152-ФЗ)
- `thank-you.html`, `404.html` — служебные страницы (app.js НЕ подключён)
- `assets/app.js` — меню, табы, модалки, валидация формы, cookie-баннер
- `assets/config.js` — `window.PLATFORMANCE_CONFIG={webhookUrl,thankYouPage}` + список `files=[...]` для динамической подгрузки `v2-*.css/js`
- `assets/styles.css` — базовые стили (синхронная загрузка, есть в `<head>`)
- `assets/v2-*.css`, `assets/v2-dom-*.js`, `assets/v2-patches*.js` — слой визуального рефайна "v2", подгружается **асинхронно** через `config.js` после первого рендера

### ⚠️ Важная архитектурная ловушка v2-слоя
`config.js` хранит явный список файлов для динамической подгрузки. **`assets/v2-dom-1-4.js` в этот список НЕ включён** — он мёртвый код, никогда не выполняется. Раньше казалось, что он отвечает за блок «Система», но реальная разметка этого блока — в `index.html` напрямую, а `v2-patches-more.js` дополняет её в рантайме (кольца, theme-классы). **Перед правкой любого визуального бага сначала проверяй, действительно ли файл, который собираешься редактировать, загружается** (сверяйся со списком `files` в `config.js`), иначе правки уйдут в никуда.

## Что сделано в сессиях до 2026-08-10 (хронология)

1. **GitHub Pages** — объяснено, как включить, дана ссылка `alexblacklucky.github.io/PLATFORMANCE/`.
2. **152-ФЗ «О персональных данных»**:
   - Обязательный чек-бокс согласия в форме (`index.html`), валидируется в `app.js`, при отправке пишет `consent_given_at` (ISO-время) и `policy_version` в скрытые поля — аудиторский след.
   - `privacy.html` дополнен: ссылки на 152-ФЗ/149-ФЗ, раздел «Согласие на обработку ПД» (ст. 9: перечень данных, цели, действия, срок 5 лет, порядок отзыва), раздел про трансграничную передачу, раздел `#cookies`.
   - Cookie-баннер в `app.js` (показ при первом визите, хранение согласия в `localStorage`), стили в `styles.css`.
3. **Юридические реквизиты оператора** внесены реальные:
   - ИП Чалова Татьяна Владимировна, ОГРНИП 322527500111310, ИНН 525604029671
   - Адрес: 603011, Нижегородская область, г. Нижний Новгород, ул. Октябрьской Революции, д. 37, помещ. п4в
   - Банк: ООО «Банк Точка», р/с 40802810601500400864, БИК 044525104, корр.счёт 30101810745374525104, КПП банка 772301001
   - Внесены в `privacy.html` (разделы «Согласие», «Контакты оператора», «Банковские реквизиты»), `index.html` (контакты, JSON-LD `Organization.legalName/taxID/identifier`, футер).
   - **Незакрытые орг. требования (не код сайта)**: уведомление в Роскомнадзор (ст. 22 152-ФЗ), назначение ответственного за обработку ПД (ст. 22.1), маркировка интернет-рекламы (ERID/ОРД) — актуально при запуске контекстной/таргетированной рекламы.
4. **Интеграция формы с Bitrix24 CRM** — выбран способ: встроенная CRM-форма Bitrix24 (`<script data-b24-form="...">`), без передачи вебхук-токена в клиентский код. Портал `b24-pdnc01.bitrix24.ru`. Пользователь создал форму «Контактные данные от 1 июля» с базовыми полями. Выдано ТЗ для агента (Claude в Chrome) на донастройку полей: канал связи, площадка (выбор маркетплейса: Wildberries/Ozon/оба/планируем), ссылка на магазин/товар, комментарий — чтобы соответствовали форме на сайте. **Статус: полностью готово.** Код вставки интегрирован 2026-08-10 (PR #23), форма `inline/4/btnjoi` встроена в `#audit .form-card`. Донастройка на портале завершена 2026-08-11 (вместе с пользователем, пошагово): подключено собственное соглашение 152-ФЗ «Согласие на обработку персональных данных (PLATFORMANCE)» с реквизитами ИП и ссылкой на `privacy.html#consent` (стандартное соглашение Битрикса отключено — было дублирование двух чек-боксов); настроен редирект при успешной отправке на `thank-you.html` (раздел «Действие после отправки», виден только в **экспертном режиме** редактора форм — тумблер внизу слева; «при неуспешной» осознанно пусто, чтобы не терять введённые данные при повторе); ответственные за лиды — Алексей и Дмитрий Безруков; название формы в CRM — «Заявка с сайта», сценарий «Контактные данные» (создаёт контакт + сделку). Тестовые лиды с прода дошли, пользователь подтвердил: «всё отлично работает».
5. **Skill `stitch::generate-design`** — был установлен глобально (`npx skills add google-labs-code/stitch-skills`), затем **полностью удалён** по просьбе пользователя (`npx skills remove --global stitch-generate-design -y`). Stitch MCP-сервер так и не был подключён (документация Google требует авторизованного доступа, пользователь столкнулся с ошибкой подключения на `app-companion-430619.appspot.com`). Если в будущем понадобится Stitch — нужно заново пройти регистрацию/настройку через `stitch.withgoogle.com` под Google-аккаунтом пользователя.
6. **Исправление радар-анимации блока «Система»**:
   - Баг: подпись-тег «Точка заказа» проваливалась под карточку «Реклама» — плавающие `.system-floater` имели фиксированные px-координаты без `z-index`, накладывались на `.system-node` (z-index:4) при некоторых ширинах экрана.
   - Решение (по выбору пользователя — концепция «радар/сонар-пульс»): узлы переставлены в кардинальные точки (верх/право/низ/лево) вокруг ядра; подпись-тег теперь `<small class="node-tag">` **внутри** кнопки-узла, а не отдельный плавающий элемент — наложение исключено структурно. Добавлены расходящиеся импульсные кольца (`.radar-pulse`) и вращающийся луч развёртки (`.radar-sweep`), пульсация ядра. `prefers-reduced-motion` отключает анимации.
   - Правки внесены в `index.html` (реально используемая разметка), `assets/styles.css` (база + fallback-позиционирование на случай сбоя загрузки v2-слоя), `assets/v2-0.css` (доп. стили радара и мобильные брейкпоинты), `assets/v2-patches-more.js` (убрана мёртвая логика создания floater-тегов).
   - Попутно найден и исправлен независимый баг: на ширине 651–880px `.system-map` схлопывался до ~46px из-за `margin:auto` без явного `width` в паре с absolute-позиционированными детьми (затрагивало весь сайт, не только новый дизайн).
   - Проверено Playwright на ширинах 1920/1024/768/480/390/360px — наложений нет, клики по узлам переключают карточку деталей корректно.
   - `assets/v2-dom-1-4.js` тоже был отредактирован для консистентности, но он не используется (см. ловушку выше) — это не баг, осознанное решение не тратить время на синхронизацию мёртвого кода.
7. **PR и публикация**: открыт и смержен `PR #1` в `main` (squash-merge, коммит `8be487e`). GitHub Pages пересобирает сайт автоматически после мерджа в `main` (обычно 1–3 минуты). Затем `PR #2` — добавление этого файла CLAUDE.md.
8. **Автопилот блока «Система»** (по выбору пользователя из предложенных идей интерактива):
   - Итоговая механика (2-я итерация по просьбе пользователя): **луч радара — источник истины**. JS ведёт луч через rAF (`SWEEP_PERIOD=20000`, полный оборот 20с), узлы стоят в кардинальных точках 0/90/180/270°, порядок в DOM совпадает с ходом луча по часовой. Узел активируется ровно в момент прохода луча (`Math.floor(apAngle/90)`), карточка деталей меняется синхронно. Полоска прогресса на активном узле = угловое расстояние до следующего узла, через CSS-переменную `--ap-progress` (scaleX).
   - Логика в `app.js`: `startSystemAutoplay/pauseSystemAutoplay/stopSystemAutoplay` + IntersectionObserver (работает только когда блок во вьюпорте, threshold .35). Hover по карте — луч замирает, уход курсора — продолжает с того же угла. Ручной клик по узлу — остановка навсегда + класс `is-user-controlled` на `.system-map`, луч плавно гаснет (opacity 0). `prefers-reduced-motion` отключает всё.
   - CSS-анимация `radarSpin` в `v2-0.css` осталась как fallback на случай, если JS не выполнится (JS отключает её через `sweepEl.style.animation='none'`), замедлена до тех же 20s.
   - ⚠️ При этом обнаружен и удалён **скрытый дублирующий автоплей в `v2-motion.js`** (интервал 4.3s, «кликал» по узлам и перезапускался после ручного клика — конфликтовал с новой логикой). В `v2-motion.js` оставлена только логика класса `is-page-scrolled`. Если появится странное двойное переключение узлов — искать второй таймер именно там.
   - Отложенные идеи интерактива (пользователь выбрал только автопилот): вспышка узла при проходе луча, частицы данных по линиям к ядру, hover-связи между узлами с микро-подписями влияния.

9. **Компактная сетка «Услуги»**: пользователь пожаловался на пустоты в бенто-сетке (featured-карточка растягивалась на 2 ряда/735px при контенте на треть высоты). Решение — переопределения в конце `v2-feedback-01.css` (загружается последним по списку в `config.js`): все шесть карточек стали одинаковыми `span 4` (3×2), `min-height:0`, уменьшены заголовки/отступы/иконки; тёмная акцент-карточка сохранена. `service-card-featured/wide` классы из `v2-patches-more.js` по-прежнему навешиваются, но нейтрализованы CSS. Версия кэш-бастера в `config.js` поднята до '11'.

## Сессия 2026-08-10 (PR #11–#22) — большой раунд правок по отзывам пользователя

Рабочий процесс сессии: ветка `claude/work-setup-ll69ax` → PR → squash-merge в `main` (пользователь сказал «Сделай сам» — PR открывает и мержит Claude). Кэш-бастер v2-слоя дошёл до `?v=15`.

1. **PR #11 — макет шире и плотнее** (референс пользователя — ctr-lab.ru): контейнер 1260→1500px, вертикальные отступы секций 88→64px, крупные заголовки уменьшены (hero h1 82→64px, h2 66→52px). Всё — блоком в конце `v2-feedback-01.css`.
2. **PR #12–#13 — регрессии/контраст**: ⚠️ выучен урок каскада — правила, дописанные в конец `v2-feedback-01.css` без медиазапроса, перебивают и более ранние медиаправила той же специфичности, и базовые вроде `.section-head.split{max-width:none}` (равная специфичность, файл позже). Из-за этого split-шапки сжались до 1040px (починено #12). В тёмном блоке аудита `v2-4c.css` перекрашивал только `p`/`span`, но не `b` — жирные слова сливались с фоном (починено #13).
3. **PR #14 — FAQ перенесён в самый конец** страницы (после «Контактов»). Рантайм-замены `PV2.id(...)` от позиции секций не зависят.
4. **PR #15 — блок «Для кого» переписан через боли клиентов** (по веб-исследованию болей селлеров WB/Ozon: реклама съедает маржу, деньги в излишках, кассовые разрывы, out-of-stock, юнит-экономика без половины расходов). 5 конкретных пунктов на вкладку вместо 3 общих.
5. **PR #16–#17 — анонимизация отчёта бренда**: файл переименован `reports/villet-analytics.html`→`reports/brand-analytics.html`, бренд — вымышленный **VELMARA SHOPP**, все 19 артикулов заменены на длинные коды без тире (формат «2 буквы + 5 цифр + 2 буквы»: VS84512AK, AT62905RW…). Цифры и формат отчёта не тронуты. ⚠️ Старая версия с реальным названием осталась в git-истории публичного репозитория — пользователь предупреждён; если попросит, нужна перезапись истории.
6. **PR #18 — подписи матрицы «Спрос × Конкуренция»** в `reports/wb-pet-niche.html`: вынесены из пузырей, тёмный текст с белым ореолом (`paint-order:stroke`), перенос до 3 строк, автоподбор позиции без наложений (функция `drawLabels`).
7. **PR #19 — подписи под скриншотами кейсов**: 16→19px + фиолетовое подчёркивание.
8. **PR #20 — секция текстовых кейсов `#cases` удалена**, разметка в `snippets/hidden-sections.html`, ссылки «Кейсы» перенацелены на `#case-gallery`.
9. **PR #21 — конверсия**: CTA-полоса «Система вместо хаоса» перенесена в середину страницы (после `#audience`); все ссылки `a[href="#audit"]` открывают форму заявки в модальном окне `<dialog class="lead-dialog">` — карточка `.form-card` переносится в диалог и возвращается при закрытии. ⚠️ Клик перехватывается на **capture-фазе** со `stopPropagation`, потому что в `app.js` есть глобальный обработчик плавной прокрутки по всем `a[href^="#"]` — без этого страница прокручивалась к `#audit` параллельно с открытием модалки.
10. **PR #22 — эта запись в память проекта.**

Проверка каждой правки — Playwright по локальному серверу (`python3 -m http.server`), скриншоты + вычисленные стили; всё смержено, ничего в работе не висит.

## Сессия 2026-08-11 (PR #23–#28) — Bitrix24 закрыт, переезд на platformance.ru

1. **PR #23** — нативная форма заменена встроенной CRM-формой Bitrix24 (код вставки от пользователя); фолбэк-контакты, если виджет не загрузился за 8с; старая форма — в `snippets/native-lead-form.html`.
2. **PR #24 — ⚠️ выучена ловушка top layer**: `showModal()` выносит `<dialog>` в браузерный top layer, который перекрывает ЛЮБЫЕ z-index — попап соглашения Bitrix24 и cookie-баннер (живут в `body`) проваливались под модалку. Решение: `leadDialog.show()` + собственное затемнение на `.lead-dialog` (fixed, z-index 1100), Esc вручную, cookie-баннер поднят до z-index 1200 (выше модалки — «Принять» можно жать, не закрывая форму). Если появится новый попап/оверлей — не использовать showModal рядом с ним.
3. **PR #25** — донастройка Bitrix24 на портале завершена (детали — в пункте 4 хронологии выше). Интеграция CRM полностью закрыта, лиды доходят.
4. **PR #26** — решение пользователя: скрытые секции остаются скрытыми насовсем (см. открытые задачи).
5. **PR #27 — переезд на platformance.ru**: домены куплены на reg.ru (заказ №216119737: platformance.ru + platformance.online + ненужные Reg.Host/DomainSSL). DNS-серверы домена пришлось менять с хостинговых `ns1/ns2.hosting.reg.ru` (reg.ru привязал к купленному хостингу автоматически) на обычные `ns1/ns2.reg.ru` — только после этого зона редактируется на странице домена. Прописаны 4 A-записи на IP GitHub Pages + CNAME www. В коде: файл `CNAME`, старый домен заменён в canonical/OG/JSON-LD/robots/sitemap/README.
6. **Конец сессии**: Pages-деплой с новым доменом прошёл успешно (проверено по журналу сборок, 19:32 МСК), но у пользователя `platformance.ru` ещё открывал заглушку хостинга Reg.Host — DNS-кэш провайдера не истёк. Рекомендованы `ipconfig /flushdns` и проверка с мобильного интернета. **Следующую сессию начать с проверки: открывается ли `platformance.ru`, пройдена ли DNS-проверка в Settings → Pages, включён ли Enforce HTTPS** — и прогнать финальную проверку (сертификат, 301 с github.io и www, canonical, работа формы Bitrix24 с прода).

## Сессия 2026-08-12 (PR #29–#32)

1. **Домен закрыт**: у пользователя «Проверка DNS продолжается» висела больше суток и Enforce HTTPS был недоступен. Проверили зону на reg.ru — она была **изначально корректной** (4 A-записи + CNAME www, CAA нет, DNSSEC выключен), бесплатный DomainSSL-сертификат reg.ru лежит в аккаунте неиспользованным и ни на что не влияет. Проблема оказалась просто затянувшейся проверкой на стороне GitHub: пока обсуждали, статус сам сдвинулся на «TLS certificate is being provisioned», через несколько минут сертификат выпустился и Enforce HTTPS включился. ⚠️ **Вывод на будущее: при зависшей DNS-проверке GitHub Pages сначала убедиться, что зона верна, и просто подождать — пересоздавать привязку домена не потребовалось.**
2. **PR #29** — новый логотип (SVG-копия присланного пользователем изображения), юр. адрес → проспект Ленина, д. 45, индекс 603135; **анимация радара больше не гаснет**: убрана логика `stopSystemAutoplay`/`is-user-controlled`, которая после ручного клика по узлу навсегда останавливала луч (это было решение прошлой сессии, пользователь его отменил).
3. **PR #30** — иконка логотипа **убрана из шапки совсем** (осталась только текстовая надпись PLATFORMANCE) на всех четырёх страницах; декоративное фиолетовое свечение карточек «Услуги» сделано светлее (`#efeaff`) и распространено на все карточки с белым фоном, кроме тёмной.
4. **PR #31** — память проекта: домен, документы 152-ФЗ.
5. **Документы 152-ФЗ подготовлены** (лежат вне репозитория, отданы в чат): приказ о назначении ответственного (ответственная — сама ИП Чалова Т.В.) и памятка по подаче уведомления в РКН с готовыми формулировками под каждое поле. **Дата начала обработки ПД — 06.10.2022** (уточнено 2026-08-18 при заполнении формы РКН, в уведомлении указана именно она). Прежняя запись «10.08.2026» относилась только к заявкам с сайта и была верна, пока уведомление касалось одного канала. Уведомление подаётся на оператора целиком, поэтому в поле идёт самая ранняя дата по всем целям — начало договорной работы с заказчиками. Пользователь сначала назвал 01.09.2022, затем сверился с ЕГРИП и поставил **06.10.2022** (дата не может быть раньше регистрации ИП; год регистрации читается из ОГРНИП 3**22**527500111310 — 2022). Первый сотрудник принят 15.03.2024, CRM-форма Bitrix24 подключена 10.08.2026 — обе даты позже и на поле не влияют.
6. **SEO-аудит** — см. новую задачу ниже.

## Известные открытые задачи на будущее

- **Скрытые секции — решение зафиксировано (2026-08-11)**: «Как начинается работа» (`#process`) и «Регламент работы» (`#rhythm`) остаются скрытыми **насовсем** — это осознанный выбор пользователя, а не «временно». Разметка хранится в `snippets/hidden-sections.html` в репозитории, чтобы сайт можно было дорабатывать в будущем; на самом сайте упоминаний быть не должно (проверено 2026-08-11: grep по всем четырём страницам — ноль следов; `v2-dom-2-2.js`/`v2-dom-2-3.js` в `config.js` — мёртвый код без видимых эффектов, оставлены для лёгкого возврата). **Не предлагать пользователю вернуть секции** — только если он сам попросит. Текстовые «Кейсы» (`#cases`) — аналогично в том же снippet-файле (PR #20).

- ~~**Домен platformance.ru**~~ — **задача закрыта 2026-08-12**: HTTPS включён (Enforce HTTPS активен, сертификат Let's Encrypt выпущен), редиректы и работа формы Bitrix24 с прода проверены пользователем вручную; ссылки в Bitrix24 (соглашение и redirect на thank-you) обновлены на `platformance.ru`. Домен `platformance.online` **переадресовывать не будем** — решение пользователя, оставляет его себе, из задач убран.

- **152-ФЗ, организационная часть — документы готовы, подача за пользователем (2026-08-12)**: подготовлены два файла (лежат вне репозитория, отданы пользователем в чат): (1) *Приказ № 1 о назначении ответственного за организацию обработки персональных данных* — ответственная **сама ИП Чалова Т.В.** (пользователь сначала выбрал «другой сотрудник», затем передумал; лист ознакомления убран за ненадобностью), осталось проставить дату и подписать; (2) *Памятка по подаче уведомления в Роскомнадзор* — порядок подачи через `pd.rkn.gov.ru` + таблица с готовыми формулировками под каждое поле формы (цели, категории данных и субъектов, действия, меры защиты, трансграничная передача — не осуществляется, БД на территории РФ). ⚠️ **Актуальный контекст закона**: с 30.05.2025 штраф за отсутствие уведомления вырос ~в 100 раз, до 300 000 ₽ для ИП/юрлиц; прежние исключения (в т.ч. «обработка только по договору») практически отменены — подавать обязаны почти все. Осталось на стороне пользователя: подписать приказ и подать уведомление.

  **Заполнение формы РКН — сессия 2026-08-18.** Уведомление заполнялось вместе с пользователем пошагово, по скриншотам портала `pd.rkn.gov.ru`. Ключевые решения, которых нет в памятке (памятка составлялась только под сайт и договоры и **устарела** — её нужно перевыпустить):
  - **Целей четыре**, каждая своим блоком (кнопка «Добавить цель обработки»), и внутри каждого блока свои категории субъектов, данных, оснований, действий и способов — это главная ловушка формы: субъекты задаются не один раз на всё уведомление, а на каждую цель: (1) «Подготовка, заключение и исполнение гражданско-правового договора» — заявки и договоры с клиентами; (2) «иная» → *оценка эффективности работы сайта и рекламных каналов (веб-аналитика)* — cookie, IP, UTM, Метрика; (3) «Продвижение товаров, работ, услуг на рынке» — рассылки; (4) «Ведение кадрового и бухгалтерского учета» — работники.
  - **Правовые основания**: блок 1 — согласие + договор; блоки 2 и 3 — только согласие; блок 4 — согласие + возложенные законом обязанности + договор.
  - **Перечень действий** одинаков во всех блоках — 13 галочек, **без «распространения»** (иначе потребуется отдельное согласие по ст. 10.1) и **без «иных действий»** (открывает обязательное текстовое поле).
  - **Способы обработки**: везде «смешанная» / «без передачи по внутренней сети юридического лица» (ИП — не юрлицо) / «с передачей по сети Интернет»; в блоке аналитики первый список — «автоматизированная».
  - **Шифровальные средства — «не используются»** (СКЗИ не внедрены; штатный HTTPS таковым не считается — из текста «средства обеспечения безопасности» формулировку про криптографическую защиту убрали, чтобы поля не противоречили). ⚠️ **Ловушка**: если поставить «используются», форма открывает поля «класс СКЗИ» (КС1–КА) и «наименование, изготовители, серийные номера» — это про сертифицированные ФСБ изделия (КриптоПро, ViPNet) с формуляром и серийником. Заявив их, оператор попадает под приказ ФСБ № 378 и инструкцию № 152: поэкземплярный учёт, журналы, ответственный за криптосредства. Ничего этого у ИП нет — вариант только «не используются», фраза про HTTPS живёт исключительно в поле «средства обеспечения безопасности».
  - **Воинский учёт не отмечаем** — ИП его вести не обязан. **Сведения о состоянии здоровья** отмечаются как спецкатегория, если принимаются больничные (основание — п. 2.3 ч. 2 ст. 10 152-ФЗ).
  - **Базы данных**: №1 — облачный Bitrix24, страна Россия, адрес ЦОДа **Москва, Коровинское шоссе, д. 44** (получен от вендора; при необходимости справка «сведения о местонахождении ЦОД» запрашивается в поддержке Битрикса), собственный ЦОД — **нет**; №2 — кадры и бухгалтерия по адресу ИП (проспект Ленина, д. 45), собственный ЦОД — **да**. GitHub Pages в перечень не входит — ПД там не хранятся, форма шлёт данные напрямую в Bitrix24. Отмечено как потенциальный риск на будущее: в логах зарубежного хостинга оседают IP посетителей; при желании закрыть вопрос — перенос на российский хостинг (~полдня, домен и почта не затрагиваются).
  - **Продвижение как цель требует отдельного согласия** (ч. 2 ст. 15 152-ФЗ): в форме Bitrix24 нужен второй **необязательный** чек-бокс «Согласен получать информационные и рекламные сообщения». Политика на сайте под это уже поправлена, чек-бокс на портале — за пользователем.
  - **Раздел «Сведения о лицах, имеющих доступ…»** касается только государственных и муниципальных информационных систем (ГИС/МИС) — у ИП их нет, блок оставляется пустым и удаляется. Битрикс24 и сторонний бухгалтер туда НЕ вписываются, это частая ошибка.
  - **Прочие поля**: срок прекращения обработки — «Условие окончания» + текст (достижение целей; отзыв согласия; истечение сроков хранения; прекращение деятельности оператора); трансграничная передача — не осуществляется; исполнитель — Чалова Т.В., ИП; сведения о безопасности по требованиям Правительства — текст со ссылками на ПП РФ № 1119 и № 687 **без указания номера уровня защищённости** (для схемы ИП это УЗ-4, но только если больничные обрабатываются на бумаге; если номера ЛН попадают в электронную бухгалтерию — УЗ-3, поэтому номер намеренно не заявлен).
  - ⚠️ **Оператор УЖЕ внесён в реестр** (обнаружено при попытке отправки 2026-08-18): портал отвечает «Оператор персональных данных уже внесён в реестр» и закрывает форму «о намерении осуществлять обработку». **Карточка в реестре: регистрационный номер 52-25-205481, уведомление зарегистрировано 21.05.2025, внесены приказом № 104 от 27.05.2025** (данные общедоступны по ч. 4 ст. 22 152-ФЗ). То есть просрочки подачи нет и штрафный риск по ст. 13.11 снят — оператор в реестре с мая 2025. Кто именно подавал, пользователь не помнит; дата за неделю до повышения штрафов 30.05.2025 намекает на бухгалтера или сервис вроде Контура — у них же может быть ключ. Подавать нужно вторую форму — **«Уведомление об изменении сведений»**, она требует реквизиты ранее поданного уведомления: **номер** (52-25-205481), **ключ** (приходил письмом при подаче — искать почту за 21–27 мая 2025 на `platformance@internet.ru` и личном ящике) и **e-mail** первичной подачи. Ключ восстанавливается письменным обращением в Управление РКН по Нижегородской области. Все заполненные формулировки переносятся в форму изменений один в один. Перед заполнением — посмотреть карточку оператора в реестре, что там заявлено сейчас.
  - **Памятка перевыпущена (редакция 2 от 2026-08-18)**, отдана пользователю в чат `.docx`: четыре цели поблочно, общие поля, готовые тексты мер по ст. 18.1/19 и ПП РФ № 1119/687, раздел ловушек, раздел про подачу через форму изменений. Лежит вне репозитория. Исходник генератора — скрипт на `docx` (npm) в скрэтчпаде сессии, при следующем перевыпуске проще написать заново.
  - **Черновик уведомления сохранён в личном кабинете `pd.rkn.gov.ru`** (раздел черновиков, открывается под ЕСИА пользователя), не отправлен. Прямая ссылка с guid в память намеренно не записана — репозиторий публичный, а из кабинета черновик открывается в два клика. Перед отправкой оставалось: вернуть «не используются» в шифровальных средствах и проверить, что база Bitrix24 и база по адресу ИП заведены обеими записями.
- 🔜 **СЛЕДУЮЩАЯ ЗАДАЧА — SEO и подготовка к рекламе (поставлена 2026-08-12)**. Пользователь планирует запуск рекламы и хочет выполнить весь SEO-план целиком. Разбор оформлен артефактом: https://claude.ai/code/artifact/eef484c9-03b9-4f4e-b4d8-784fae0e6f88
  - **Результаты аудита кода**: техчасть в порядке (title/description, один H1 + 13 H2, `robots.txt`, `sitemap.xml`, canonical, OG, JSON-LD Organization/WebSite/Service, alt у всех 7 изображений, HTTPS). **Аналитики нет вообще** — единственное совпадение по слову «counter» оказалось счётчиком слайдера кейсов (`.cs-counter`). Не подключены: Яндекс.Вебмастер, Google Search Console, Яндекс.Метрика; верификационных метатегов в `index.html` нет.
  - **Почему не находят**: домену несколько дней (переезд 11.08.2026, прежний вес остался на `github.io`), сайт не заявлен поисковикам, и это одностраничный лендинг — ранжируется максимум по одной группе запросов.
  - **Шаги на стороне пользователя**: Яндекс.Вебмастер (подтвердить метатегом, добавить sitemap, **обязательно указать регион** — Нижний Новгород/Россия, отправить на переобход) → Google Search Console (HTML-тег, sitemap) → Яндекс.Метрика (Вебвизор + цель «Отправка заявки») → карточка в Яндекс.Бизнесе.
  - **Что делать в коде, когда пользователь пришлёт коды**: вставить метатеги верификации Яндекса и Google в `index.html`, код Метрики на все страницы, расширить JSON-LD (услуги, ценовой диапазон, `FAQPage` из блока вопросов — даёт расширенный сниппет), создать отдельные посадочные страницы под группы запросов (карточки/контент, реклама, финансы, поставки, выход на площадку) + раздел статей.
  - **Ожидания по срокам, озвученные пользователю**: индексация 2–4 недели, первые позиции 2–4 месяца, конкуренция по коммерческим запросам 6–12 месяцев. Для быстрых заявок рекомендован Директ + Яндекс.Бизнес, SEO — вдолгую.
- При появлении контекстной/таргетированной рекламы — добавить маркировку ERID/ОРД (актуализируется вместе с задачей выше).
- Если пользователь попросит полностью вычистить реальное название бренда из истории репозитория (сейчас старый отчёт доступен в старых коммитах) — потребуется перезапись git-истории (`git filter-repo`/BFG) и force-push `main`; делать только по явной просьбе.
